# Public API のベースURL
VITE_API_BASE_URL=/api/v1

# 開発時: モックAPIは既定で有効。false で無効化して実APIへ接続する
# 本番ビルド: 既定で無効。デモ目的で有効にする場合のみ true を明示する
VITE_USE_MOCK=true

# dev server が /api をプロキシする先（実API接続時に設定）
# VITE_API_PROXY=http://localhost:8080
